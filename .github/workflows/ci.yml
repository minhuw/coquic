name: Per-Commit CI

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  steward-schema:
    name: Steward Schema
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@1d87d45818068401a10cf16bdc5f00b24994a83f
        with:
          summarize: false

      - name: Check generated Steward contract
        run: nix develop -c uv run --project steward python steward/schema/generate_types.py --check

      - name: Test Steward contract compatibility
        run: nix develop -c uv run --project steward python -m pytest steward/tests/test_public_schema.py

  format:
    name: Format
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@1d87d45818068401a10cf16bdc5f00b24994a83f
        with:
          summarize: false

      - name: Format Check
        run: nix develop -c pre-commit run clang-format --all-files --show-diff-on-failure

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@1d87d45818068401a10cf16bdc5f00b24994a83f
        with:
          summarize: false

      - name: Lint
        env:
          COQUIC_CLANG_TIDY_DIFF_BASE: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          COQUIC_CLANG_TIDY_DIFF_HEAD: ${{ github.sha }}
        run: |
          if [ "${COQUIC_CLANG_TIDY_DIFF_BASE}" = "0000000000000000000000000000000000000000" ]; then
            COQUIC_CLANG_TIDY_DIFF_BASE="$(git rev-parse HEAD^)"
            export COQUIC_CLANG_TIDY_DIFF_BASE
          fi
          nix develop .#lint -c ./scripts/run-clang-tidy.sh --diff

  compliance:
    name: RFC Compliance
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@1d87d45818068401a10cf16bdc5f00b24994a83f
        with:
          summarize: false

      - name: Duvet Report
        run: nix develop -c ./scripts/compliance --ci

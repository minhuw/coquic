name: Perf

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  perf:
    name: Advisory QUIC Perf
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      # Remove this when GitHub runner gets docker engine >= v28.1.0.
      - uses: docker/setup-docker-action@v5
        with:
          version: version=v28.3.0

      - name: Show Docker Version
        run: |
          docker version

      - name: Perf Harness Contract
        run: bash tests/nix/perf_harness_test.sh

      - name: Run CoQUIC Perf CI Matrix (NewReno + CUBIC + BBR + Copa)
        id: perf_coquic
        continue-on-error: true
        run: PERF_RESULTS_ROOT=.bench-results/coquic bash bench/run-host-matrix.sh --preset ci

      - name: Run quic-go Paired Baseline
        id: perf_quic_go
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/quic-go \
            PERF_CLIENT_IMPL=quic-go \
            PERF_SERVER_IMPL=quic-go \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run quinn Paired Baseline
        id: perf_quinn
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/quinn \
            PERF_CLIENT_IMPL=quinn \
            PERF_SERVER_IMPL=quinn \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run picoquic Paired Baseline
        id: perf_picoquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/picoquic \
            PERF_CLIENT_IMPL=picoquic \
            PERF_SERVER_IMPL=picoquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run MSQUIC Paired Baseline
        id: perf_msquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/msquic \
            PERF_CLIENT_IMPL=msquic \
            PERF_SERVER_IMPL=msquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run quiche Paired Baseline
        id: perf_quiche
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/quiche \
            PERF_CLIENT_IMPL=quiche \
            PERF_SERVER_IMPL=quiche \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run quicly Paired Baseline
        id: perf_quicly
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/quicly \
            PERF_CLIENT_IMPL=quicly \
            PERF_SERVER_IMPL=quicly \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run Google QUICHE Paired Baseline
        id: perf_google_quiche
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/google-quiche \
            PERF_CLIENT_IMPL=google-quiche \
            PERF_SERVER_IMPL=google-quiche \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run TQUIC Paired Baseline
        id: perf_tquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/tquic \
            PERF_CLIENT_IMPL=tquic \
            PERF_SERVER_IMPL=tquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run mvfst Paired Baseline
        id: perf_mvfst
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/mvfst \
            PERF_CLIENT_IMPL=mvfst \
            PERF_SERVER_IMPL=mvfst \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run s2n-quic Paired Baseline
        id: perf_s2n_quic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/s2n-quic \
            PERF_CLIENT_IMPL=s2n-quic \
            PERF_SERVER_IMPL=s2n-quic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run xquic Paired Baseline
        id: perf_xquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/xquic \
            PERF_CLIENT_IMPL=xquic \
            PERF_SERVER_IMPL=xquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run aioquic Paired Baseline
        id: perf_aioquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/aioquic \
            PERF_CLIENT_IMPL=aioquic \
            PERF_SERVER_IMPL=aioquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run ngtcp2 Paired Baseline
        id: perf_ngtcp2
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/ngtcp2 \
            PERF_CLIENT_IMPL=ngtcp2 \
            PERF_SERVER_IMPL=ngtcp2 \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run LSQUIC Paired Baseline
        id: perf_lsquic
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/lsquic \
            PERF_CLIENT_IMPL=lsquic \
            PERF_SERVER_IMPL=lsquic \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Run Neqo Paired Baseline
        id: perf_neqo
        continue-on-error: true
        run: |
          PERF_RESULTS_ROOT=.bench-results/neqo \
            PERF_CLIENT_IMPL=neqo \
            PERF_SERVER_IMPL=neqo \
            PERF_CONGESTION_CONTROLS=default \
            bash bench/run-host-matrix.sh --preset ci

      - name: Publish Perf Summary
        if: always()
        run: |
          python3 scripts/render-perf-comparison.py \
            --manifest coquic=.bench-results/coquic/manifest.json \
            --manifest quic-go=.bench-results/quic-go/manifest.json \
            --manifest quinn=.bench-results/quinn/manifest.json \
            --manifest picoquic=.bench-results/picoquic/manifest.json \
            --manifest msquic=.bench-results/msquic/manifest.json \
            --manifest quiche=.bench-results/quiche/manifest.json \
            --manifest quicly=.bench-results/quicly/manifest.json \
            --manifest google-quiche=.bench-results/google-quiche/manifest.json \
            --manifest tquic=.bench-results/tquic/manifest.json \
            --manifest mvfst=.bench-results/mvfst/manifest.json \
            --manifest s2n-quic=.bench-results/s2n-quic/manifest.json \
            --manifest xquic=.bench-results/xquic/manifest.json \
            --manifest aioquic=.bench-results/aioquic/manifest.json \
            --manifest ngtcp2=.bench-results/ngtcp2/manifest.json \
            --manifest lsquic=.bench-results/lsquic/manifest.json \
            --manifest neqo=.bench-results/neqo/manifest.json \
            --event-name "${GITHUB_EVENT_NAME}" \
            --commit "${GITHUB_SHA}" \
            --json-out .bench-results/perf-results.json >> "${GITHUB_STEP_SUMMARY}"

      - name: Configure Demo SSH
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          COQUIC_DEMO_REMOTE_SSH_KEY: ${{ secrets.COQUIC_DEMO_REMOTE_SSH_KEY }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s' "${COQUIC_DEMO_REMOTE_SSH_KEY}" > "${RUNNER_TEMP}/coquic-demo.key"
          cat > "${HOME}/.ssh/known_hosts" <<'EOF'
          coquic.minhuw.dev ssh-rsa 
          coquic.minhuw.dev ecdsa-sha2-nistp256 AAAAE2VjZHNhLXNoYTItbmlzdHAyNTYAAAAIbmlzdHAyNTYAAABBBCjTT5KdjyCQ3G04Z8mUUOkXAqsdDuU4K5bB3jXi5B3aIzYK+2f0TT0XE3G0/r/KmZoSHYxb21TtjykFa47XFSA=
          coquic.minhuw.dev ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILi5VjtrGcYlzyYEXd4/PzQJXBjZ028DA6nJ7Xu50CJh
          EOF
          chmod 600 "${RUNNER_TEMP}/coquic-demo.key" "${HOME}/.ssh/known_hosts"

      - name: Upload Perf Results To Demo
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          if [ ! -f .bench-results/perf-results.json ]; then
            echo "missing .bench-results/perf-results.json" >&2
            exit 1
          fi
          scp \
            -P 22 \
            -i "${RUNNER_TEMP}/coquic-demo.key" \
            -o BatchMode=yes \
            -o ConnectTimeout=10 \
            -o ServerAliveInterval=10 \
            -o ServerAliveCountMax=3 \
            -o StrictHostKeyChecking=yes \
            -o UserKnownHostsFile="${HOME}/.ssh/known_hosts" \
            .bench-results/perf-results.json \
            minhuw@coquic.minhuw.dev:/tmp/coquic-perf-results.json
          ssh \
            -p 22 \
            -i "${RUNNER_TEMP}/coquic-demo.key" \
            -o BatchMode=yes \
            -o ConnectTimeout=10 \
            -o ServerAliveInterval=10 \
            -o ServerAliveCountMax=3 \
            -o StrictHostKeyChecking=yes \
            -o UserKnownHostsFile="${HOME}/.ssh/known_hosts" \
            minhuw@coquic.minhuw.dev \
            "sudo install -m 644 /tmp/coquic-perf-results.json /opt/coquic-demo/current/site/perf-results.json && rm -f /tmp/coquic-perf-results.json"

      - name: Fail On Perf Matrix Errors
        if: always()
        run: |
          failed=0
          check_step() {
            local label="$1"
            local outcome="$2"
            if [ "${outcome}" != "success" ]; then
              echo "${label} perf step finished with outcome: ${outcome}" >&2
              failed=1
            fi
          }
          check_step "CoQUIC" "${{ steps.perf_coquic.outcome }}"
          check_step "quic-go" "${{ steps.perf_quic_go.outcome }}"
          check_step "quinn" "${{ steps.perf_quinn.outcome }}"
          check_step "picoquic" "${{ steps.perf_picoquic.outcome }}"
          check_step "MSQUIC" "${{ steps.perf_msquic.outcome }}"
          check_step "quiche" "${{ steps.perf_quiche.outcome }}"
          check_step "quicly" "${{ steps.perf_quicly.outcome }}"
          check_step "Google QUICHE" "${{ steps.perf_google_quiche.outcome }}"
          check_step "TQUIC" "${{ steps.perf_tquic.outcome }}"
          check_step "mvfst" "${{ steps.perf_mvfst.outcome }}"
          check_step "s2n-quic" "${{ steps.perf_s2n_quic.outcome }}"
          check_step "xquic" "${{ steps.perf_xquic.outcome }}"
          check_step "aioquic" "${{ steps.perf_aioquic.outcome }}"
          check_step "ngtcp2" "${{ steps.perf_ngtcp2.outcome }}"
          check_step "LSQUIC" "${{ steps.perf_lsquic.outcome }}"
          check_step "Neqo" "${{ steps.perf_neqo.outcome }}"
          exit "${failed}"

      - name: Upload Perf Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quic-perf-results
          path: .bench-results
          include-hidden-files: true
          if-no-files-found: warn

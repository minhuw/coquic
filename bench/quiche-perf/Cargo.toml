[package]
name = "quiche-perf"
version = "0.1.0"
edition = "2021"

[dependencies]
mio = { version = "1.1", features = ["net", "os-poll"] }
quiche = "0.28.0"
ring = "0.17.14"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

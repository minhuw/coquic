[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "coquic"
version = "0.1.0"
description = "Python wrapper for the CoQUIC C FFI"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

[tool.setuptools]
packages = ["coquic"]

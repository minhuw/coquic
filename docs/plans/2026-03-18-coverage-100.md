# QUIC Codec Full Coverage Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Raise the current QUIC codec coverage report to 100% for both LLVM region and branch coverage while preserving runtime behavior.

**Architecture:** Add narrow failing tests around the remaining uncovered branches in the frame, packet, and varint codecs, then make minimal production cleanups only where a branch is defensive or structurally unreachable. Re-run the full coverage pipeline after each batch until the report is fully green.

**Tech Stack:** Zig build system, C++20, GoogleTest, LLVM `llvm-cov`/`llvm-profdata`

---

### Task 1: Record the current hotspots and coverage targets

**Files:**
- Modify: `docs/plans/2026-03-18-coverage-100-design.md`
- Modify: `docs/plans/2026-03-18-coverage-100.md`

- [ ] **Step 1: Confirm the baseline report**

Run: `nix develop -c zig build coverage`
Expected: PASS, with totals still below 100% for region and branch coverage.

- [ ] **Step 2: Inspect the report for the remaining hotspots**

Run: `python - <<'PY' ... PY` or equivalent local inspection against `coverage/lcov.info`
Expected: identify remaining uncovered branches in `src/quic/frame.cpp`, `src/quic/packet.cpp`, and `src/quic/varint.cpp`.

### Task 2: Drive missing frame coverage with TDD

**Files:**
- Modify: `tests/quic_frame_test.cpp`
- Modify: `src/quic/frame.cpp` (only if a branch remains structurally unreachable after tests)

- [ ] **Step 1: Write failing frame tests for currently uncovered decode and serialize paths**

Add focused tests for:
- additional ACK decode failures and ECN handling branches
- frame-family serialization failures with currently unvisited inputs
- rarely used decoder success branches for frame variants already supported by the codec

- [ ] **Step 2: Run only the new frame tests and verify they fail for the expected reason**

Run: `nix develop -c zig build test -- --gtest_filter=QuicFrameTest.*`
Expected: FAIL because the new paths are not yet fully exercised or need minimal cleanup.

- [ ] **Step 3: Make the smallest code change needed to cover any truly unreachable branch**

Keep behavior identical; only simplify or consolidate defensive structure if tests cannot reach a branch meaningfully.

- [ ] **Step 4: Re-run the frame tests and confirm they pass**

Run: `nix develop -c zig build test -- --gtest_filter=QuicFrameTest.*`
Expected: PASS.

### Task 3: Drive missing packet and varint coverage with TDD

**Files:**
- Modify: `tests/quic_packet_test.cpp`
- Modify: `tests/quic_varint_test.cpp`
- Modify: `src/quic/packet.cpp` (only if a branch remains structurally unreachable after tests)
- Modify: `src/quic/varint.cpp` (only if a branch remains structurally unreachable after tests)

- [ ] **Step 1: Write failing packet and varint tests for the uncovered branches**

Add focused tests for:
- packet-number and connection-id validation branches
- packet-type and header-path selection branches not yet exercised
- the 8-byte varint prefix path and any remaining decode/validation branch outcomes

- [ ] **Step 2: Run only the new packet and varint tests and verify they fail for the expected reason**

Run: `nix develop -c zig build test -- --gtest_filter=QuicPacketTest.*:QuicVarIntTest.*`
Expected: FAIL because the remaining uncovered paths are not yet exercised or need minimal cleanup.

- [ ] **Step 3: Make the smallest code change needed to cover any truly unreachable branch**

Preserve all current packet and varint semantics.

- [ ] **Step 4: Re-run the packet and varint tests and confirm they pass**

Run: `nix develop -c zig build test -- --gtest_filter=QuicPacketTest.*:QuicVarIntTest.*`
Expected: PASS.

### Task 4: Verify the entire suite and coverage totals

**Files:**
- Modify: `tests/quic_frame_test.cpp`
- Modify: `tests/quic_packet_test.cpp`
- Modify: `tests/quic_varint_test.cpp`
- Modify: `src/quic/frame.cpp` (if needed)
- Modify: `src/quic/packet.cpp` (if needed)
- Modify: `src/quic/varint.cpp` (if needed)

- [ ] **Step 1: Run the full test suite**

Run: `nix develop -c zig build test`
Expected: PASS.

- [ ] **Step 2: Run the full coverage pipeline**

Run: `nix develop -c zig build coverage`
Expected: PASS and `coverage/html/index.html` reports `100.00%` for both region and branch coverage totals.

- [ ] **Step 3: Re-check the generated artifacts**

Inspect: `coverage/html/index.html`, `coverage/lcov.info`
Expected: no remaining uncovered branches in project sources.

# Synthetic completed task

Make the fixture-only change and preserve raw evidence. The harmless raw note
contains `/synthetic/archive` and `SYNTHETIC_TOKEN_NOT_A_CREDENTIAL` solely to
demonstrate that raw bytes are disclosed without content filtering.

// Generated by scripts/build-search-index.mjs. Do not edit by hand.

import type { SiteSearchItem } from '@/lib/search-index';

export const generatedDocSearchItems = [
  {
    "id": "docs-overview",
    "title": "CoQUIC Documentation",
    "href": "/docs",
    "section": "Start",
    "description": "CoQUIC Documentation These documents describe the CoQUIC project API and integration model. Reference material that is not project documentation lives under references/. That in...",
    "kind": "docs",
    "headings": [
      "API Hierarchy",
      "Contents",
      "Reference Corpus"
    ],
    "keywords": [
      "references/",
      "include/coquic/",
      "wraps",
      ".",
      "or",
      "references/rfc/",
      "references/papers/"
    ],
    "body": "CoQUIC Documentation These documents describe the CoQUIC project API and integration model. Reference material that is not project documentation lives under references/. That includes RFC text, Internet-Drafts, papers, audits, and older research notes used by tooling or implementation work. API Hierarchy The public API is the compatibility boundary exported from include/coquic/. Inside that boundary, callers choose the lowest layer that matches how much control they need. include/coquic/ public API +-- coquic::core Lowest layer: sans-I/O endpoint, typed inputs, typed effects. +-- coquic::quic Transport facade: endpoint, connection, and stream handles over core. +-- coquic::http3 Application protocol layer: HTTP/3 state that emits QUIC connection inputs. -- C FFI C ABI wrapper over the sans-I/O endpoint for native bindings. coquic::quic wraps coquic::core. coquic::http3 composes with either core or quic: it does not own sockets or endpoints, and it feeds work back to the matching QUIC connection. Contents Public API: API overview and compatibility boundary. C FFI API: C ABI, ownership, package names, and event loop. Core API: sans-I/O endpoint, inputs, effects, and timers. QUIC Facade API: transport facade over the core endpoint. HTTP/3 API: request/response layer and QUIC input handoff. Runtime Integration: event-loop checklist for callers. Reference Corpus QUIC RFC and Internet-Draft text: references/rfc/ Research papers: references/papers/ Historical notes and audits: references/.md Use references/rfc/` as the source path for RAG indexing and protocol citations."
  },
  {
    "id": "docs-api-public-api",
    "title": "Public API",
    "href": "/docs/api/public-api",
    "section": "API Surface",
    "description": "Public API CoQUIC exposes its public C++ API from include/coquic/. Code outside the library should include those headers instead of reaching into src/. #include \"coquic/coquic.h...",
    "kind": "docs",
    "headings": [
      "Layer Map",
      "Layer Selection",
      "Event-Loop Model",
      "Compatibility Boundary",
      "Build Notes"
    ],
    "keywords": [
      "include/coquic/",
      "src/",
      "coquic::core",
      "coquic::quic",
      "core",
      "coquic::http3",
      "quic",
      "core::Result",
      "http3",
      "InboundDatagram",
      "timer_expired",
      "ConnectionCommand",
      "quic::Connection",
      "SendDatagram",
      "Result::next_wakeup",
      "Endpoint::next_wakeup",
      "coquic::core::TimePoint",
      "include/",
      "tests/api/public_api_test.cpp"
    ],
    "body": "Public API CoQUIC exposes its public C++ API from include/coquic/. Code outside the library should include those headers instead of reaching into src/. #include \"coquic/coquic.h\" // core + quic + http3 Use narrower headers when a caller only needs one layer: #include \"coquic/core.h\" #include \"coquic/quic.h\" #include \"coquic/http3.h\" The public API is the compatibility boundary, not a fourth runtime layer. Within that boundary, CoQUIC exposes three API layers: coquic::core: sans-I/O QUIC endpoint API for runtimes, tests, interop harnesses, and bindings. coquic::quic: a convenience transport facade over core with endpoint, connection, and stream objects. coquic::http3: HTTP/3 request and response API that translates HTTP/3 work into QUIC connection inputs. The headers under include/coquic/ are the intended compatibility surface. Types and helpers under src/ remain implementation details. The C ABI wrapper is documented separately as the C FFI API. Use it for C consumers and native language bindings that need opaque handles, explicit result ownership, and pkg-config or CMake package metadata. Layer Map include/coquic/ public API +-- coquic/core.h coquic::core: lowest sans-I/O QUIC endpoint. +-- coquic/quic.h coquic::quic: optional facade that wraps core endpoint commands. -- coquic/http3.h coquic::http3: HTTP/3 state machine that emits QUIC connection inputs. At runtime the data flow is: HTTP/3 API emits core::ConnectionInput values v QUIC Facade API, optional forwards connection work to core v Core API consumes endpoint inputs and returns effects v Caller runtime owns sockets, timers, routing, files, and threads The facade layer is optional. A runtime can call core directly, or use quic to reduce manual construction of connection commands. HTTP/3 remains separate protocol state and feeds work into the selected QUIC connection. Layer Selection Choose core when the integration needs complete control over routing, timers, connection handles, packet effects, and event-loop behavior. Choose quic when the integration wants a small transport facade for connections and streams while still processing core::Result effects. Choose http3 when the integration wants request/response state that emits QUIC connection inputs. HTTP/3 does not own UDP sockets or a QUIC endpoint. Event-Loop Model The caller owns the runtime: sockets, timers, address routing, file I/O, and threading. CoQUIC consumes inputs and returns effects. The usual loop is: Pass inbound UDP bytes to an endpoint with InboundDatagram. Pass timer expiry with timerexpired. Pass application writes with ConnectionCommand or the quic::Connection helpers. Send every returned SendDatagram effect through the runtime socket. Deliver received stream data, DATAGRAM data, lifecycle, state, and diagnostic effects to the application. Arm the runtime timer from Result::nextwakeup or Endpoint::nextwakeup. Every endpoint method takes a coquic::core::TimePoint. Use one runtime clock consistently for all calls on an endpoint. Compatibility Boundary Stable surface: Headers in include/coquic/. Namespaces coquic::core, coquic::quic, and coquic::http3. Public value types, enums, endpoint facades, and result/effect types declared by those headers. Internal surface: Anything under src/. Test hooks and support headers. Generated demo artifacts and benchmark result JSON. Build Notes Inside this repository, zig build adds include/ to the project library, executables, and tests. Public API smoke tests live in tests/api/publicapitest.cpp`. nix develop -c zig build test Packaging and installation of exported headers for external consumers is still separate from the in-repo build."
  },
  {
    "id": "docs-api-core",
    "title": "Core API",
    "href": "/docs/api/core",
    "section": "API Surface",
    "description": "Core API The coquic::core layer is the lowest public API. It is a sans-I/O endpoint: the caller owns UDP sockets, address routing, timers, file I/O, and threading. CoQUIC consum...",
    "kind": "docs",
    "headings": [
      "Endpoint Configuration",
      "Inputs",
      "Effects",
      "Example",
      "Diagnostics"
    ],
    "keywords": [
      "coquic::core",
      "coquic::quic",
      "Use",
      "core::EndpointConfig",
      "role",
      "supported_versions",
      "verify_peer",
      "retry_enabled",
      "application_protocol",
      "identity",
      "transport",
      "zero_rtt",
      "qlog",
      "tls_keylog_path",
      "emit_shared_receive_stream_data",
      "enable_packet_inspection",
      "allow_peer_address_change",
      "core::TransportConfig",
      "core::EndpointInput",
      "OpenConnection",
      "InboundDatagram",
      "PathMtuUpdate",
      "ConnectionCommand",
      "TimerExpired",
      "core::ConnectionInput",
      "SendStreamData",
      "SendDatagramData",
      "ResetStream",
      "StopSending",
      "CloseConnection",
      "RequestKeyUpdate",
      "RequestConnectionMigration",
      "core::Result",
      "effects",
      "next_wakeup",
      "local_error",
      "send_continuation_pending",
      "core::Effect",
      "SendDatagram",
      "ReceiveStreamData",
      "ReceiveDatagramData",
      "PeerResetStream",
      "PeerStopSending",
      "StateEvent",
      "ConnectionLifecycleEvent",
      "PeerPreferredAddressAvailable",
      "ResumptionStateAvailable",
      "ZeroRttStatusEvent"
    ],
    "body": "Core API The coquic::core layer is the lowest public API. It is a sans-I/O endpoint: the caller owns UDP sockets, address routing, timers, file I/O, and threading. CoQUIC consumes typed inputs and returns typed effects. This is the foundation used directly by low-level integrations and indirectly by the coquic::quic facade. HTTP/3 work ultimately returns to this layer as QUIC connection inputs. #include \"coquic/core.h\" Use core when the integration needs direct control over connection handles, route handles, timers, packets, and application effects. Endpoint Configuration core::EndpointConfig configures one endpoint: role: client or server. supportedversions: QUIC versions, defaulting to version 1. verifypeer: peer certificate verification policy. retryenabled: server retry policy. applicationprotocol: ALPN value. identity: server certificate and key material. transport: flow-control, PMTU, DATAGRAM, ACK, migration, grease, and congestion-control settings. zerortt: 0-RTT attempt/allow policy and application context. qlog: optional qlog output directory. tlskeylogpath: optional TLS key log output. emitsharedreceivestreamdata: receive-data ownership mode. enablepacketinspection: packet inspection effect emission. allowpeeraddresschange: peer address migration policy. core::TransportConfig contains the transport-level defaults. Important settings include idle timeout, UDP payload size, PMTU probing limits, connection ID limit, stream and connection flow-control windows, DATAGRAM frame size, and congestion-control algorithm. Inputs core::EndpointInput is a variant of: OpenConnection: client connection creation. InboundDatagram: UDP payload received from the runtime. PathMtuUpdate: runtime PMTU observation. ConnectionCommand: application action for an existing connection. TimerExpired: runtime timer expiry. Connection commands carry core::ConnectionInput, a variant of: SendStreamData SendDatagramData ResetStream StopSending CloseConnection RequestKeyUpdate RequestConnectionMigration Effects Every endpoint method returns core::Result. A result contains: effects: typed actions and events. nextwakeup: next timer deadline. localerror: synchronous local API error, if any. sendcontinuationpending: more send work can be produced without waiting for network input. core::Effect is a variant of: SendDatagram: bytes the runtime must send on a UDP route. ReceiveStreamData: stream bytes delivered to the application. ReceiveDatagramData: DATAGRAM frame payload delivered to the application. PeerResetStream and PeerStopSending: peer stream-control signals. StateEvent: handshake-ready, handshake-confirmed, or failed state changes. ConnectionLifecycleEvent: connection created, accepted, or closed. PeerPreferredAddressAvailable: server preferred address advertisement. ResumptionStateAvailable: TLS resumption state for persistence. ZeroRttStatusEvent: 0-RTT attempt result. PacketInspection: optional decoded packet metadata and payload snapshots. NewTokenAvailable: address-validation token from the peer. Use helper extractors when only one effect class matters: auto datagrams = coquic::core::senddatagrams(result); auto states = coquic::core::stateevents(result); auto streams = coquic::core::receivestreamevents(result); auto datagramevents = coquic::core::receivedatagramevents(result); Example #include \"coquic/core.h\" #include cstddef #include cstdint #include initializerlist #include vector std::vector std::byte bytes(std::initializerlist std::uint8t values) { std::vector std::byte out; out.reserve(values.size()); for (auto value : values) { out.pushback(staticcast std::byte (value)); } return out; } void openclientconnection() { coquic::core::Endpoint endpoint({ .role = coquic::core::Role::client, .verifypeer = false, .applicationprotocol = \"coquic\", }); const auto now = coquic::core::Clock::now(); auto result = endpoint.openconnection( coquic::core::OpenConnection{ .connection = { .sourceconnectionid = bytes({0xc1, 0x01}), .initialdestinationconnectionid = bytes({0x83, 0x41}), .servername = \"localhost\", }, .initialroutehandle = 7, }, now); for (const auto &datagram : coquic::core::senddatagrams(result)) { // Send datagram.bytes on the UDP route identified by datagram.routehandle. } } Diagnostics Endpoint::connectiondiagnostics() returns lightweight connection state for debug views and tests. It includes handshake status, current version, active paths, active streams, and retired streams. Diagnostics are observability data, not protocol commands."
  },
  {
    "id": "docs-api-quic",
    "title": "QUIC Facade API",
    "href": "/docs/api/quic",
    "section": "API Surface",
    "description": "QUIC Facade API The coquic::quic layer wraps the core endpoint with endpoint, connection, and stream facade objects. It keeps the same caller-owned event-loop model while reduci...",
    "kind": "docs",
    "headings": [
      "Endpoint",
      "Connection",
      "Stream",
      "Example",
      "Ownership Rules"
    ],
    "keywords": [
      "coquic::quic",
      "core::ConnectionCommand",
      "coquic::core",
      "core::Result",
      "quic::Endpoint",
      "core::Endpoint",
      "quic::Connection",
      "quic::Stream"
    ],
    "body": "QUIC Facade API The coquic::quic layer wraps the core endpoint with endpoint, connection, and stream facade objects. It keeps the same caller-owned event-loop model while reducing manual construction of core::ConnectionCommand values. Use it as an ergonomic transport layer above coquic::core; it does not replace the core result/effect model. #include \"coquic/quic.h\" Use this layer when the caller wants a transport API for connection and stream operations but still wants to process core::Result effects directly. Endpoint quic::Endpoint owns a core::Endpoint internally. It exposes: connect(ClientConfig, TimePoint): open a client connection. connection(ConnectionHandle): create a connection facade for a handle. receivedatagram(InboundDatagram, TimePoint): deliver UDP input. updatepathmtu(PathMtuUpdate, TimePoint): deliver PMTU updates. timerexpired(TimePoint): deliver timer expiry. advance(EndpointInput, TimePoint): pass through generic core input. nextwakeup(): current timer deadline. connectioncount() and connectiondiagnostics(): endpoint state. Connection quic::Connection is a lightweight handle back into an endpoint. It exposes: stream(StreamId): create a stream facade. advance(ConnectionInput, TimePoint): pass through generic connection input. sendstream(StreamId, bytes, fin, TimePoint). senddatagram(bytes, TimePoint). resetstream(StreamId, errorcode, TimePoint). stopsending(StreamId, errorcode, TimePoint). close(errorcode, reasonphrase, TimePoint). requestkeyupdate(TimePoint). Each method returns core::Result. The caller must still send returned datagrams and process returned effects. Stream quic::Stream is a convenience handle for a single stream ID: send(bytes, fin, TimePoint) finish(TimePoint) reset(errorcode, TimePoint) stopsending(errorcode, TimePoint) Example #include \"coquic/quic.h\" #include cstddef #include cstdint #include initializerlist #include vector std::vector std::byte bytes(std::initializerlist std::uint8t values) { std::vector std::byte out; out.reserve(values.size()); for (auto value : values) { out.pushback(staticcast std::byte (value)); } return out; } void sendonstream() { coquic::quic::Endpoint endpoint({ .core = { .role = coquic::core::Role::client, .verifypeer = false, .applicationprotocol = \"coquic\", }, }); auto connected = endpoint.connect( coquic::quic::ClientConfig{ .core = { .sourceconnectionid = bytes({0xc1, 0x01}), .initialdestinationconnectionid = bytes({0x83, 0x41}), .servername = \"localhost\", }, .initialroutehandle = 7, }, coquic::core::Clock::now()); for (const auto &datagram : coquic::core::senddatagrams(connected.result)) { // Send datagram.bytes through the runtime socket. } auto stream = connected.connection.stream(0); const auto payload = bytes({0x68, 0x65, 0x6c, 0x6c, 0x6f}); auto writeresult = stream.send(payload, true, coquic::core::Clock::now()); for (const auto &datagram : coquic::core::senddatagrams(writeresult)) { // Send datagram.bytes through the runtime socket. } } Ownership Rules Facade objects do not own sockets and do not send network bytes by themselves. They are handles into the endpoint. Keep the endpoint alive while using connections or streams created from it."
  },
  {
    "id": "docs-api-http3",
    "title": "HTTP/3 API",
    "href": "/docs/api/http3",
    "section": "API Surface",
    "description": "HTTP/3 API The coquic::http3 layer translates HTTP/3 requests and responses into QUIC connection inputs. It does not own a UDP socket or a QUIC endpoint. Use it as application p...",
    "kind": "docs",
    "headings": [
      "Data Types",
      "Client",
      "Server",
      "Integration Notes"
    ],
    "keywords": [
      "coquic::http3",
      "coquic::core",
      "coquic::quic",
      "or",
      "and",
      "quic_inputs"
    ],
    "body": "HTTP/3 API The coquic::http3 layer translates HTTP/3 requests and responses into QUIC connection inputs. It does not own a UDP socket or a QUIC endpoint. Use it as application protocol state above the QUIC transport. It can be paired with the low-level coquic::core API or with the coquic::quic facade. #include \"coquic/http3.h\" Use this layer together with coquic::core or coquic::quic: Drive the QUIC endpoint from the runtime. Pass QUIC results into http3::Client or http3::Server. Feed returned quicinputs back to the matching QUIC connection. Use clientendpointconfig() and serverendpointconfig() to set endpoint role and ALPN to h3. Data Types HTTP messages use simple value types: Field: one header field. Headers: vector of fields. RequestHead: method, scheme, authority, path, content length, headers. ResponseHead: status, content length, headers. Request: head, body, trailers. Response: interim heads, final head, body, trailers. Settings: local HTTP/3 and QPACK settings. Errors use http3::ErrorCode and http3::Error. API calls that can fail use http3::Result T . Client http3::Client exposes: submitrequest(Request): enqueue a request and return its stream ID. onquicresult(core::Result, TimePoint): consume QUIC events and emit HTTP/3 work. poll(TimePoint): produce pending HTTP/3 work without new QUIC input. hasfailed(): terminal client failure state. ClientUpdate contains: quicinputs: inputs to feed to the matching QUIC connection. responses: completed response events. requesterrors: request cancellation/error events. haspendingwork: poll again without waiting for network input. terminalfailure: HTTP/3 layer failure. handledlocalerror: whether a QUIC local error was consumed. #include \"coquic/http3.h\" void submithttp3request(coquic::http3::Client &client) { auto submitted = client.submitrequest(coquic::http3::Request{ .head = { .method = \"GET\", .scheme = \"https\", .authority = \"example.test\", .path = \"/\", }, }); if (!submitted.hasvalue()) { // Inspect submitted.error(). return; } coquic::core::Result ready; ready.effects.pushback(coquic::core::StateEvent{ .connection = 1, .change = coquic::core::StateChange::handshakeready, }); auto update = client.onquicresult(ready, coquic::core::Clock::now()); for (auto &input : update.quicinputs) { // Feed input to the QUIC connection with core::ConnectionCommand or // quic::Connection::advance. } } Server http3::Server exposes: onquicresult(core::Result, TimePoint): consume QUIC events and emit HTTP/3 work. poll(TimePoint): produce pending HTTP/3 work. hasfailed(): terminal server failure state. ServerConfig provides request handlers: requestheadhandler: optional early response after the request head. requesthandler: response after the full request is available. fallbackrequesthandler: explicit fallback path. ServerUpdate contains QUIC inputs, cancelled request events, pending-work state, terminal failure state, and local-error handling state. #include \"coquic/http3.h\" #include optional coquic::http3::Server makeserver() { return coquic::http3::Server({ .requestheadhandler = [](const coquic::http3::RequestHead &head) - std::optional coquic::http3::Response { if (head.path == \"/healthz\") { return coquic::http3::Response{ .head = {.status = 204, .contentlength = 0}, }; } return std::nullopt; }, .requesthandler = [](const coquic::http3::Request &request) { return coquic::http3::Response{ .head = { .status = 200, .contentlength = request.body.size(), }, .body = request.body, }; }, }); } Integration Notes HTTP/3 objects are per-connection protocol state. They do not route QUIC connection handles by themselves. Feed each quicinputs item to the QUIC connection that produced the corresponding HTTP/3 event stream."
  },
  {
    "id": "docs-api-integration",
    "title": "Runtime Integration",
    "href": "/docs/api/integration",
    "section": "Runtime",
    "description": "Runtime Integration CoQUIC is driven by the caller runtime. The library does not own the event loop, UDP sockets, timers, worker threads, or persistent storage. Event Loop A typ...",
    "kind": "docs",
    "headings": [
      "Event Loop",
      "Sending Datagrams",
      "Timers",
      "Errors",
      "Checklist",
      "Build Notes"
    ],
    "keywords": [
      "coquic::core::InboundDatagram",
      "Endpoint::input_datagram",
      "quic::Endpoint::receive_datagram",
      "SendDatagram",
      "Result::next_wakeup",
      "Endpoint::next_wakeup",
      "timer_expired",
      "advance_connection",
      "quic::Connection",
      "quic::Stream",
      "coquic::core::TimePoint",
      "bytes",
      "route_handle",
      "ecn",
      "is_pmtu_probe",
      "send_continuation_pending",
      "core::LocalError",
      "terminal_failure",
      "include/coquic/",
      "src/",
      "quic_inputs",
      "include/",
      "tests/api/public_api_test.cpp"
    ],
    "body": "Runtime Integration CoQUIC is driven by the caller runtime. The library does not own the event loop, UDP sockets, timers, worker threads, or persistent storage. Event Loop A typical runtime loop: Read UDP datagrams from the socket. Convert each datagram to coquic::core::InboundDatagram. Call Endpoint::inputdatagram or quic::Endpoint::receivedatagram. Send every returned SendDatagram effect. Dispatch stream, DATAGRAM, lifecycle, state, token, qlog, and diagnostic effects to the application. Arm the next timer from Result::nextwakeup or Endpoint::nextwakeup. On timer expiry, call timerexpired. For application writes, call advanceconnection or the quic::Connection and quic::Stream helpers. Every endpoint method takes coquic::core::TimePoint. Use one monotonic runtime clock consistently for all calls on an endpoint. Sending Datagrams SendDatagram effects are the only network output. The runtime sends: bytes: UDP payload. routehandle: selected local route, if present. ecn: ECN codepoint requested by the endpoint. ispmtuprobe: whether the datagram is a PMTU probe. Send all produced datagrams before waiting for more socket or timer input. If sendcontinuationpending or Endpoint::hassendcontinuationpending() is true, call back into the endpoint without blocking so pending send work can continue. Timers Always arm the runtime timer from the most recent result: Prefer Result::nextwakeup when processing a call result. Use Endpoint::nextwakeup() when the runtime needs the current endpoint deadline outside a call result. If no wakeup is present, no timer is currently needed. Timer callbacks should call timerexpired(now) and process the returned effects exactly like network or application input. Errors core::LocalError reports synchronous API misuse or locally detected failure. It does not send UDP bytes by itself. Treat it as an application signal and keep processing any returned effects that are valid for the call. For HTTP/3, terminalfailure and hasfailed() report protocol-layer failure. Request-level failures are returned as request error or cancellation events. Checklist Include public headers from include/coquic/. Do not include internal headers from src/. Keep one monotonic clock source per endpoint. Preserve route-handle identity between inbound datagrams and outbound sends. Send every SendDatagram effect. Re-arm the timer after each endpoint call. Feed HTTP/3 quicinputs to the matching QUIC connection. Persist resumption state and address-validation tokens only if the application wants to reuse them. Treat diagnostics as observability data, not protocol commands. Build Notes Inside this repository, zig build adds include/ to the project library, executables, and tests. Public API smoke tests live in tests/api/publicapitest.cpp and are run by: nix develop -c zig build test Packaging and installation of exported headers for external consumers is still separate from the in-repo build."
  }
] as const satisfies readonly SiteSearchItem[];
